import React from "react";
import { LoginContainer } from "./styles";

const Login: React.FC = () => {
  return (
    <LoginContainer>
      Login Page
    </LoginContainer>
  );
};

export default Login;
